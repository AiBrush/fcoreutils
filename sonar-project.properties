sonar.projectKey=AiBrush_fcoreutils
sonar.organization=aibrush
sonar.sources=src
sonar.tests=tests
sonar.exclusions=target/**,assembly/**/*.o
# Hash module has inherent structural duplication across algorithms (SHA-256, SHA-512, MD5, etc.)
# and between Rust File-based and raw syscall I/O paths — both intentional by design.
# Tests have natural structural similarity across different test scenarios.
sonar.cpd.exclusions=**/hash/**,**/tests.rs,**/tests/**
