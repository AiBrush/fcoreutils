# Local development: override with RUSTFLAGS="-C target-cpu=native" for max perf
# CI release builds set target-cpu per-platform in .github/workflows/release.yml
# Keeping this empty ensures portable binaries by default
